package com.nedellis.calvinium

import io.kotest.core.spec.style.FunSpec

class ExecutorSuite :
    FunSpec({
        //    test("Executor runs op") {
        //        val executor = Executor()
        //
        //        executor.run(UniqueOperation(UUID.randomUUID(), Operation("KEY", Put("RESULT"))))
        // shouldBe "RESULT"
        //        executor.run(UniqueOperation(UUID.randomUUID(), Operation("KEY", Get))) shouldBe
        // "RESULT"
        //        executor.run(UniqueOperation(UUID.randomUUID(), Operation("KEY", Delete)))
        // shouldBe "RESULT"
        //    }
    })
